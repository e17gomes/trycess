import { LoadingComponent } from "~/components/ui/atoms/loadingComponent";

const Loading = () => {
  return (
    <div className="min-h-screen flex justify-center items-center bg-accent">
      <LoadingComponent />
    </div>
  );
};

export default Loading;
